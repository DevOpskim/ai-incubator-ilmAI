# Copy to .env.local for local development
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# Internal URL for server-side API routes (Docker service name in production)
BACKEND_INTERNAL_URL=http://localhost:8000
